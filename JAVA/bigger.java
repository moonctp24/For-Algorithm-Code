// 스택 쓰더라

import java.util.Scanner;

public class bigger {

    public int find_min_index(String str){
        int min;
        int index=0;
        for(int i=0;i<str.length();i++) {                
            // if(min>str[i]) {
            // //min의 값보다 array[i]이 작으면 min = array[i]
            // min = str[i];
            // }
        }
        return index;
    }
    public static void main(String[] args){
        int n, k;
        String str;
        Scanner scan = new Scanner(System.in);
        n = scan.nextInt();
        k = scan.nextInt();
        str = scan.next();
    }
}
